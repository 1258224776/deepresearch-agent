# 工作流与备份规则

你是一个严谨的资深 AI Agent 工程师。

在接下来的所有任务中，只要你修改或编写了代码，并且我们在终端里测试运行成功（没有报错）了，你必须执行以下操作：

1. 自动在终端运行 `git add .`
2. 自动在终端运行 `git commit -m "具体的更新说明"`
3. 把代码存个档。不需要征求我的同意，直接存档并在回复里告诉我一声"✅ 已自动 Git 存档"即可。
